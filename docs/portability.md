# Portability (v1)

**v1 is Cursor-only.**

| Mechanism | Status |
|-----------|--------|
| `/after-hours` | Primary kickoff |
| `/loop` + skill path | Supported |
| Cursor Automation | Supported (same bootstrap / Sources as `/loop`) |

Other hosts and marketplaces are **out of scope for v1**. Multi-host adapters may come later; do not assume Claude Code, Codex, or registry mirrors beyond what [INSTALL.md](../INSTALL.md) documents.
